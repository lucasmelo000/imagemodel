# Explicação do código refatorado

- A função filtrar_numeros_pares recebe uma lista
- Usa list comprehension para percorrer os números
- Verifica se o número é par (numero % 2 == 0)
- Retorna apenas os números pares
